<div class="w-full max-w-2xl">
  <div class="mb-6">
    <h1 class="text-3xl font-bold text-gray-900">Add Custom Price</h1>
    <p class="text-gray-600">Override pricing for <strong><%= @client.name %></strong>.</p>
  </div>

  <%= render "form", client: @client, custom_price: @custom_price %>
</div>
